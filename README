iwork自动保存的小程序。
    此程序同时也是个launcher，运行程序后，会出现一个选单，选择启动一个iwork程序，新建的文档只要保存过一次，每隔10分钟就会自动保存一次。
    也可以右键点iwork文档，打开方式选这个程序，然后文档就会被相应的iwork程序打开，并且每10分钟自动保存一次。

    注意，每次自动保存的文档就是源文档，而不是一个备份文件。多文档应该是支持的，没有测试。
